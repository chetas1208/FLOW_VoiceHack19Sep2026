apiVersion: v1
kind: Service
metadata:
  name: flow-api
  namespace: flow
  labels:
    app.kubernetes.io/name: flow-cloud
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: flow
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: flow-cloud
    app.kubernetes.io/component: api
  ports:
    - {name: http, port: 80, targetPort: http, protocol: TCP}
