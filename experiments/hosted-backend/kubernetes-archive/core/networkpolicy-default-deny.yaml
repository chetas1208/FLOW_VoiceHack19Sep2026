# Everything in the namespace is denied unless another policy allows it.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: flow
  labels:
    app.kubernetes.io/part-of: flow
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
