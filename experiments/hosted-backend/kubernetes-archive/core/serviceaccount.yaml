# One identity for every FLOW cloud workload. None of them talk to the Kubernetes API, so no token is mounted.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: flow-cloud
  namespace: flow
  labels:
    app.kubernetes.io/part-of: flow
automountServiceAccountToken: false
