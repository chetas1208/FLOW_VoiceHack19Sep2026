# Same image pin as ../kustomization.yaml; keep them in sync (scripts/lint_k8s.py checks this).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flow
resources:
  - ../../../migrate
images:
  - name: ghcr.io/chetas1208/flow-cloud
    newTag: "0.2.0"
