# TEMPLATE ONLY -- every value is a placeholder. Never commit real values.
# Create the real Secret out-of-band (kubectl create secret, External Secrets, Sealed Secrets, Vault, ...):
#   kubectl -n flow create secret generic flow-cloud-secrets \
#     --from-literal=DATABASE_URL=... --from-literal=REDIS_URL=... --from-literal=FLOW_JWT_KEYS=... \
#     --from-literal=KEDA_DATABASE_URL=... --from-literal=FLOW_METRICS_TOKEN=...
apiVersion: v1
kind: Secret
metadata:
  name: flow-cloud-secrets
  namespace: flow
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg://CHANGE_ME_USER:CHANGE_ME_PASSWORD@CHANGE_ME_HOST:5432/flow?sslmode=require"
  KEDA_DATABASE_URL: "postgresql://CHANGE_ME_USER:CHANGE_ME_PASSWORD@CHANGE_ME_HOST:5432/flow?sslmode=require"
  REDIS_URL: "rediss://:CHANGE_ME_PASSWORD@CHANGE_ME_HOST:6379/0"
  # kid:secret[,kid2:secret2]; the first entry signs, all verify (rotation). >= 32 bytes each.
  FLOW_JWT_KEYS: "k1:CHANGE_ME_GENERATE_WITH_openssl_rand_base64_48"
  FLOW_METRICS_TOKEN: "CHANGE_ME_OPTIONAL"
